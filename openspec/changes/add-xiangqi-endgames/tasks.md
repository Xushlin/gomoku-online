# tasks — add-xiangqi-endgames

## 0. 先把上一个收尾 —— 这是硬顺序,不是礼节

- [ ] `add-xiangqi-manual`(PR #146)合并并归档。**本变更的 `xiangqi-manual` / `web-xiangqi-manual` 两份 delta 全部写成 ADDED,正是为了不去 MODIFIED 一条还不在 live spec 里的要求** —— 那需要手工合并两个未归档变更,而本仓库记着那条:MODIFIED 整体替换,retype 是无关句子被静默回退的方式。
- [ ] 归档之后 `openspec validate --strict` 通过,再动代码。

## 1. 数据:1634 局全部结构校验,再决定后面做不做

- [x] 六辑 1634 个 id 收齐(`/gupu/3..8/`,document order,去重)。
- [ ] 全部取下 —— **串行 + 延时,只抓一次**,之后进仓库不再联网。**后台 shell 没有网络出口**(踩过:558 次全失败而单独请求 200,只有文件数暴露了这件事),所以分批在前台跑。
- [ ] 结构校验全 1634 局:一帅一将、将/士在九宫、相/象在各自的点、无两子同格、招法串长度、每一手起点在推演局面上有子、首手是红子。
- [ ] **报出分布**:结构问题几条、子数区间、手数区间、谱评三态各多少、满盘几局、首手非红几局。**这一步的结果决定后面还做不做** —— 抽样 30 局是 0 问题,而 1634 局不是 30 局的放大。
- [ ] 一辑一份数据文件,记来源与版权判断(明清刊本;白话注解不导入)。

## 2. 后端

- [ ] `XiangqiManualLine` 加起始局面列 + 一个 guarded 迁移,把既有 31 条回填成标准开局。**迁移的 `Down` 能整表回退是因为数据可从仓库产物复现** —— 这条理由写进迁移,与 `AddXiangqiManualLines` 同理。
- [ ] 谱评加第三态「谱未标注」。
- [ ] 播种器按起始局面分两条路径;**非标准那条明确是结构校验**,推演器只搬子不判合法性,命名上不许冒充规则引擎。
- [ ] **先看到红**:改坏一手的起点让它落在空格上 → 报出第几手;把标准开局那条的某一手改非法 → 仍按旧路径拒(证明旧路径没退化)。
- [ ] 目录端点支持列出多部谱;单谱端点下发起始局面。
- [ ] 断言:播种前后 `Rooms/Games/Moves/UserGameStats` 仍是 0(古谱不是对局,这条不能因为多了几辑就丢)。

## 3. 前端

- [ ] 谱的列表页 → 单谱目录 → 学习页三层;分组允许只有一层,不给残局编局号。
- [ ] 学习页首帧用起始局面。**判据是首帧子数等于起始局面子数** —— 而不是「代码里传了」:改这条之前棋盘从标准开局重放,一条 10 子的残局会渲染成 32 子加几步棋,**一个看起来完全正常的错盘面**。
- [ ] 变异:让学习页忽略起始局面 → 那条断言必须红。
- [ ] 局面性质(残局 / 满盘)从子数推;两类都要在测试样本里。
- [ ] i18n:`manual.*` 补键,两份 locale 齐备。
- [ ] 界面**不许**出现任何「解对了」文案,也不许有「接着自己走」的入口 —— 一条源码规则钉住它。

## 4. 在真浏览器里看

- [ ] 一条 10 子残局:首帧就是 10 个子;走到末手再回来逐格相同。
- [ ] 一条 32 子的让先局:不被标成残局。
- [ ] 一条谱未标注的:谱评那行显示「谱未标注」,不显示红优。
- [ ] **375 px** —— `add-xiangqi-manual` 欠着的那条一并补上;它欠着是因为浏览器面板全程隐藏(`innerWidth` 是 0),所以这一条的前提是面板可显示。

## 5. 收尾

- [ ] 后端 + 前端全绿,lint 0,两个 tsconfig 0,build 0。
- [ ] 归因证明新页面是懒的(不是因为写了 `loadComponent`)。
- [ ] `CLAUDE.md` 延期表加两条,**因为写在规格正文里的触发条件没有人读**:
      - 「走法合法」这条判据的拆除条件 = `XiangqiRules` 能从给定局面开局;
      - 「解对了」判定的拆除条件 = 长将/重复局面规则落地 **且** AI 能从给定局面走棋。
