# tasks

## 1. 出口本身

- [x] `RoomRepository.leave(roomId)` / `dissolve(roomId)`,以及 hub 的 `leaveRoom(roomId)`。
- [x] `open()` 里那个 `addListener(_republish)` 改成**只注册一次**,离开时摘掉。
      今天重复注册是幂等的 —— **而无害恰好是这种东西活下来的方式**。
- [x] `close()` 终于有调用方了;确认它做的三件事(摘监听、停 hub、dispose `_live`)在
      「离开一个房间再进另一个」的路径上都是对的 —— **确认,别假设**:
      停掉 hub 之后下一个房间要能重新连上。
- [x] 走哪条路由由**服务端规则**决定:等待中房间的房主走 `/dissolve`,其余走 `/leave`。
      **两个方向都要测** —— 少了后半条,「永远调 /dissolve」也能通过前半条。

## 2. 确认弹窗

- [x] 进行中要问、等待中不问,判据与 web 端同一条。文案 `game.leave-confirm.*` 全都在包里。
- [x] 用 `showDialog`,不手搭覆盖层。
- [x] 选「留下」MUST NOT 发出任何离开调用 —— **一个负面断言要配一条前置断言**证明
      那一刻确实处在「进行中」,否则它对「弹窗根本没出现」也是绿的。

## 3. `RoomDissolved`

- [x] 订阅它,导航回该棋种的大厅。
- [x] `hub_contract_test` 的订阅数 2 → 3,走查应当仍然绿(**确认**)。
- [x] 那条走查是一边倒的这件事,写进它自己的注释里 —— 现在只有 spec 里有。

## 4. 判据(真后端)

- [x] 集成测试:判据是**分支**不是座位 —— 实测两种状态下离开都**不腾空座位**,
      所以那条原判据是错的。改成:房主离开等待中的房间 → 房间不见了;客人离开 → 房间仍在。
- [x] 集成测试:等待中的房间由房主离开 → **那个房间在列表里不见了**(被解散)。
- [x] 集成测试:进 A、离开、进 B,让 A 里有人走一步(用那个 Node 脚本)→ **B 的盘面不变**。
      **这一条在 `fix-mobile-hub-inbound` 之前不可能失败**,所以它现在才写得出来。
- [x] **正面对照:把「离开时摘监听 / LeaveRoom」去掉,看上面那条红。**

## 5. 走查与不回归

- [x] `flutter analyze` 零问题;`flutter test` 全绿;`i18n_keys_test` 覆盖新增的文案键。
- [x] `integration_test/play_a_move_test.dart` 与 `xiangqi_test.dart` MUST 通过,
      **每一个匹配器与期望值逐字未变**。
- [x] 有条件的话在真安卓上按一次「离开」—— 这一轮里真机发现了三个测试看不见的问题。

## 6. 收尾

- [x] `JOURNAL.md` 一条。
- [x] `CLAUDE.md`:手机端那节**加一行** —— 离开房间要告诉服务端,而路由分支是服务端的规则。
- [x] PR 里报净改动行数。
