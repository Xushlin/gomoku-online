# tasks — play-from-position

## 0. 顺序

- [x] `add-xiangqi-endgames` 的归档(#149)合并。**这一条不是礼节** —— 本变更要 `MODIFIED` 的东西目前还不在 live spec 里,而手工合并两个未归档变更正是无关句子被静默回退的方式。
- [x] 两份 delta 现在都是 `ADDED`,所以归档后 `validate --strict` 应当直接过;过不了就说明有东西漂了,先查再动代码。—— 实测 `validate play-from-position --strict` 绿,而它红过一次:新加的一条 requirement 把 SHALL 写在第二段,**校验器只读第一段**。

## 1. 先把内核那条不变量改对,再碰象棋

- [x] 新接口 `IPositionalStartRules`,与 `IDealtGameRules` **并列**:设置由调用方选定、由规则校验。
- [x] `Room` 的 `needsSetup` 改成「两者之一」。**两个方向的抛都保留** —— 变异:把「不要设置却给了」那条删掉,必须有测试红。
- [x] 校验发生在**存下来之前**:变异 —— 让校验在 `new Game(...)` 之后跑,「房间留在 Waiting」那条必须红。
- [x] 一条遍历注册表的走查:`IPositionalStartRules ⇒ !IsRated`,与既有 `IsRated ⇒ SeatCount == 2` 并排。
- [x] **两种设置来源都要在样本里** —— 否则「两种来源」在单一种类上恒真。

## 2. 象棋:抽取,而不是复制

- [x] 把走子合法性与胜负判定抽成共用的一份;`XiangqiRules` 的**对外行为不变**。
- [x] **既有象棋测试一条不改地通过** —— 跑过了才写:断言与夹具都没动,通过的是原样的那批。
- [x] `XiangqiEndgameRules`:`IPositionalStartRules` + `IFirstSeatRules`,`IsRated => false`,走子委托给共用那份。
- [x] 一条断言:同一个局面 + 同一步棋,两个棋种判断一致,而样本用**两条路径都能到达**的局面(标准开局)。

## 3. 设置的形状与校验

- [x] 设置编码起始局面(90 字符盘面串)与先走方 —— 与古谱那两列同一个表示,**不新造第二种**。
- [x] 校验:长度、恰好一帅一将、将/士在九宫、**相/象在各自的相位**、先走方在 `[0, SeatCount)`。
- [x] **每一条都要先看到红**,而且报出的是**哪一条**不满足。相位那条是先写测试、看它红、再补实现。
- [x] 变异:让校验失败时静默退回标准开局 → 必须有测试红(那种坏表现成「这局怎么是开局」)。
- [x] 相位那条**量过全量才写**:1665 条起始局面里违例 0 条,士也全在九宫 X 的 5 个点上(没收紧,规格只要求九宫)。它是象棋的规则,不是从样本归纳的约定 —— 那个区别要紧。

## 4. 建房带线路 id

- [x] `CreateRoomCommand` 多一个可选的线路 id;服务端据它取起始局面与先走方。
- [x] **请求里不许有盘面串** —— 一条「字段恰好是这四个」的反射断言钉住它。要拦的不是 `StartPosition` 这个名字:叫 `Board` / `Fen` 一样是那个口子。
- [x] 两个方向都校验:键与 id 必须同时给或同时不给。判据取自**类型**而非键,并有一条遍历注册表的走查(两支都在样本里)。
- [x] 不存在的线路 → 清楚的拒绝(HTTP 400),而**拒绝发生在造房间之前**;不合法的库内局面同样造不出房间。

## 5. 那条**故意**会红的

- [x] `The_unrated_games_are_tictactoe_doudizhu_and_wakeng` 红了。**这是设计好的信号**。
- [x] 每个成员不计分的理由都写进去了:一字棋是「必和」,斗地主 / 挖坑是「按分结算」,残局是「开局就不公平」——**三种不同的理由**。
- [x] 另一条**没有预告**的「恰好」也红了:`IFirstSeatRules` 此前只有挖坑。答案是「同一种东西」—— 两者都是「设置说了算」,而设置从哪来由两个接口分别承载。接口不拆,名单加一个。

## 6. 前端

- [x] 房间页与回放页按「象棋族」挑棋盘,而不是单一键。
- [x] 大厅按 manifest 声明的**伴生棋种键**列房间 —— 否则「摆此局对弈」开出来的房间谁都找不到。没声明伴生键的游戏仍然只发一次请求。
- [x] 房间行的纹章从伴生键回到主棋种 —— 查不到的键画出空数组,而那**不抛、不报、不红,只是不见**。
- [x] 学习页多一个「摆此局对弈」入口;源码规则收窄成「不许判定」,而**新增一条正面规则**:残局房必须写明平台不判和棋。两条规则的两半都做过正控制。
- [x] 残局房界面写明平台不判和棋;i18n 两份 locale 齐备。
- [x] 房间状态多下发一个 `chosenSetup` —— 客户端画不出残局就得靠它。发牌那份走另一个字段,一个 DTO 都不带,而那条既有的反射断言按设计红了(见下)。
- [x] 375 px:**先读了 `innerWidth`(0 → `resize_window` 后 375)**,再量。`scrollWidth == clientWidth == 375`,零元素越界。

## 7. 在真浏览器里看

- [x] 开一局黑先走的残局(线路 169,`firstSeat=1`):**红方 90 格全部 disabled**,黑方 5 格可点(它的 5 个子)—— 两侧都量了,否则「0 可点」在任何情况下都成立。
- [x] 黑真的先动:將 (2,5)→(1,5) 落下,回合转给红。
- [x] 一步**在标准开局下非法、在残局下合法**的走子被接受:红俥 (1,2)→(4,2) —— 标准开局那格是空的。
- [x] 反方向:一步**在标准开局下合法、在残局下非法**的走子被拒 —— 另开一局(线路 118),黑象 (0,6)→(2,8) 的象眼上压着红兵。棋盘没动、回合没转、库里只有 1 手。**目标格是空的,所以客户端确实把它发出去了**(点自己的子只会重新选子,发不出去)。
- [x] 认输能用:按钮 62×**44** px,CDK 确认框,`endReason=Resigned`,房间 Finished。
- [x] 这一局**不进阶梯**:`xiangqi-endgame` 排行榜 0 条。**而正控制第一次是失败的** —— `GET /api/users/{id}` 在计分的象棋局之后也是 0,所以那个读数什么都没证明;换成排行榜端点,计分那局是 1220 / 1180,残局那局仍然 0 条。
- [x] 那句「平台不判和棋」在界面上看得见(375 px 下宽 311 px,不溢出)。
- [ ] 截图:**没有**。面板全程未显示,`screenshot` 超时;布局读数仍然有效(这是记录在案的规则),所以量的是读数。

## 8. 收尾

- [x] 后端 1573 绿,前端 993 绿,lint 0,build 0;初始包 410.19 kB(预算 480,+0.23 kB)。
- [x] 「走法合法性判据欠着」那条延期项**重新量过了,而答案不是假定的那个**:1665 条走强路径,**1658 条每一半手都合法,7 条被拒**,七条理由完全相同 —— 那一手把自己的将送进被将的位置。逐一看过一条(`077蛛网空悬` 第 3 手 帥 (9,4)→(9,3),黑卒就在 (8,3)),**是数据错,不是规则严**。所以欠账只还一半:能力有了,换路径之前要先决定那 7 条怎么办。这个结论钉成了一条「恰好 7 条、且每条都是自杀」的测试,而不是一句注释。
