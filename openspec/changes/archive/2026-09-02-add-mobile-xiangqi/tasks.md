# tasks

**前置:`add-mobile-game-catalog` 已落地并归档。** 目录、三层路由、`rows`/`cols`、`Room.gameKey`、`Move.fromRow/fromCol` 都在那一笔里。

## 1. 局面重放

- [x] 移植 `positionAfter`:**19 行,一条规则都没有**。`cells[to] = cells[from]; cells[from] = null;`
- [x] 初始摆法由「后排次序 + 关于中路镜像对称」推出,**MUST NOT 手打 32 个格子**。
- [x] `RED` / `BLACK` 两个**命名常量**。`Stone.Black` 在象棋里是红。
      **不留任何裸的 `== 0` 当「红」用** —— 那正是将来有人来「修正」它的地方。
- [x] 单测:吃子后该格只剩走过来的子、全盘子数减一;开局 32 子、每方 16 子、
      各兵种数目正确(1 将 2 士 2 象 2 马 2 车 2 炮 5 卒)、关于中路镜像对称。
- [x] 单测:重放实现里搜不到任何走法判断。
- [x] **正面对照:把后排次序里两个子对调,看「镜像对称」或「兵种数目」那条红。**

## 2. 象棋画笔

- [x] 10×9 交叉点、河界、九宫斜线、14 个字形。**没有星位。**
- [x] 进棋盘注册表 —— **目录里「哪些画得出来」的唯一真源**,不绕过它硬塞。
- [x] 字形是 CJK,**盒不等于墨**:`CLAUDE.md` 记着 CJK 行盒高约 1.45× 字号,多出来的是无字的上伸部。
      按盒定尺寸会「看着放得下其实放不下」。**判据是 375 px 下满盘 32 子不溢出**,不是算式看起来对。
- [x] 375 px 实测一次 —— **开局满盘 32 子**,不是空盘。空盘通过每一条布局断言。

## 3. 走子

- [x] 交互:选中 → 落点。**点自己的子是重新选中,永不成为一步走子。**
- [x] `MatchHub.movePiece(roomId, fromRow, fromCol, row, col)` —— **加方法,不给 `MakeMove` 加参数**。
      SignalR 不套用可选参数缺省值,多送少送都在绑定层被拒且低于日志级别,两头都看不见。
- [x] 座位读法按 `gameKey` 分派。**不按座位数** —— 象棋与五子棋都是两个座位,按座位数恰好分不开。
- [x] **正面对照:把 `RED` / `BLACK` 互换,看「象棋房说红 / 黑」那条红;
      再把分派改成按座位数,看「分派依据是棋种」那条红。**

## 4. 集成测试(真后端)

- [x] 建象棋房 → 对手加入 → **下一步真实开局(炮二平五)并要求服务端接受**。
      这就是初始摆法的判据:跟权威比,不跟另一份副本比。
- [x] 选中一个子、落点是它走不到的**空格** → 客户端**发出去** → 服务端拒绝 → 界面报错。
      **落点 MUST NOT 是自己的子**(那是重新选中,什么也不发)。
- [x] 吃子走一步,盘上子数减一 —— **在单测里,不在集成测试里,而这是能力限制不是偷懒**:
      吃子要先让黑方走一步,而集成测试只驱动一个座位,走子又只存在于 hub(没有 REST 落子端点)。
      单测 `a capture removes the piece that was there` 钉住它(31 子、黑方 15)。

## 5. 走查与不回归

- [x] 目录走查里那个「**恰好 1 个启用**」会变成 2。**那正是它该红的时刻** ——
      改数字时确认象棋是**通过棋盘注册表**进去的。
- [x] 上一笔写在类注释里的欠账(`rows != cols` 没有生产调用方)**在这一笔兑现** —— 把那段注释更新掉。
- [x] `test/layering_test.dart`:新增文件在 `ui/game/` 与 `data/models/` 下,现有四条规则应当自动覆盖 ——
      **确认,别假设。**
- [x] `test/view_model_notify_test.dart` 的 ViewModel 计数若变,**改数字并确认新那个 `extends ViewModel`**。
- [x] `integration_test/play_a_move_test.dart` MUST 通过,**每一个匹配器与期望值逐字未变**
      (五子棋那条路本笔不该动它 —— 如果动了,说明改到了不该改的地方)。
- [x] `flutter analyze` 零问题;`flutter test` 全绿。

## 6. 收尾

- [x] `JOURNAL.md` 一条。
- [x] `CLAUDE.md`:手机端那节改**一行** —— 两个棋种,象棋的局面由客户端无规则重放得出,
      `Stone.Black` 读作红。**只改一行**,这个文件每次会话整份加载。
- [x] **归档顺序:`add-mobile-game-catalog` 先,本笔后。** 两笔都只 ADDED、没有 MODIFIED。
- [x] PR 里报净改动行数。
