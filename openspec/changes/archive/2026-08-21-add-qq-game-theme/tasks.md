# tasks — add-qq-game-theme

## 1. 皮肤本体

- [x] `tokens.css` 追加 `[data-theme='qq-game']` 与 `.dark` 两块。
- [x] `DefaultThemeService` 一行 `register('qq-game')`。
- [x] `header.theme.qq-game` 双语键,i18n 对齐测试绿。

## 2. 默认值

- [x] `DEFAULT_THEME` → `'qq-game'`。
- [x] **两个方向都断言**:没存过 → 拿到 `qq-game`;存过 `'material'` → 仍然是 material。
      少了后一条,一个把所有人都改掉的实现在前一条下也是绿的。
- [x] 无效主题名的回退指向新默认。

## 3. 那三个词汇 token

- [x] `--color-on-surface` / `--color-well` / `--color-on-well`,中性值是**关系**而非字面量。
- [x] `panel` / `panel-flat` / `bar` 说出自己的前景色;`well` 有自己的填充与前景。
- [x] 现有三套主题画出来一模一样(中性值 = 今天的行为)。

## 4. 对比度

- [x] 明暗两套 × 六个前景色,按**渐变每一档**量,全部 ≥ 4.5。
- [x] 探针配正面控制(同色时必须掉到 4.5 以下)。
- [x] 记下方向问题:深字在浅渐变上取最暗档,浅字在深渐变上取最亮档。

## 5. 验收

- [x] `git diff --name-only` 里 MUST NOT 有任何组件文件,MUST NOT 有任何 `themes/*.ts`。
- [x] `npm run lint` 绿,打印 **4 themes**(主题清单从 CSS 推导,所以新主题自动进校验)。
- [x] `tsconfig.app.json` + `tsconfig.spec.json` 两个都绿(`tsconfig.json` 编译零个文件,不算)。
- [x] `npm run test:ci` 绿。
- [x] `npm run build` 在预算内,并记下确切数字。

## 6. 计划之外

- [x] **审稿稿的浅色大厅落地不成立。** shell 根把 `--color-text` 铺给整页,而深青地面配
      暖墨字读不了、配浅字则牌面上读不了。改成「暗色一套才是深青大厅,浅色一套是竹席桌面」。
      **一张每块样张各有自己作用域的设计稿,验不出「整页共享一个前景色」这种约束。**

- [x] **`extend-theme-tokens` 的共现走查对这三个 token 是盲的。** 它按现有 class 推导,
      而现有设计把「页面底色」和「凹陷面」、「整页前景」和「牌面前景」各自合成了一个。
      共现走查治好了「凭想象发明角色」,治不了「现有设计合并了两个概念」。

- [x] **差点凭一个测量假象改掉两个颜色。** 注入 `*{transition:none}` 阻止不了一个**已经在
      飞行中**、又因 pane 不合成而永远冻住的过渡 —— 那时读到的渐变色标是插值中间值。
      每种模式各自重新加载才干净。

- [x] **而随后我把「页面是错的」这个结论也判错了。** 重新加载后那个数字**复现了**,
      问题在我手算时取了渐变**最亮**那一档 —— 深色文字的最差情形是**最暗**那一档。
      两次都是我错,方向相反。**一个复现不出来的数字不能拿去改配色;而一个复现得出来的
      数字,先怀疑自己的算法。**

- [x] **`#8a5a12` 在 `tokens.css` 里有两处** —— `ink` 的哑金也是这个值。不加块限定的
      替换会**悄悄改掉另一套主题**;断言 `count == 1` 拦住了。
