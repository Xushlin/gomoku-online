# tasks

## 1. 事件集

- [x] `SoundEvent` 枚举,名字与 web 的 `SOUND_EVENTS` 一一对应。
- [x] 走查:读 `frontend-web/.../sound.tokens.ts`,断言集合**相等**(不是「包含」)。
      **正面对照:删掉一个事件,看它红。**

## 2. 合成

- [x] 正弦 / 方波 + 指数衰减包络 → `Int16List` PCM。
- [x] 事件 → 音符序列的映射,参数移植自 web 的 `minimal` 包。
- [x] PCM → WAV 字节(16-bit mono,采样率写进头里)。

## 3. 播放

- [x] 选一个插件:**最主流、最少原生代码**(#192 刚因为插件的原生构建栽过)。
      从 `Uint8List` 播,不落盘。
- [x] 即发即忘:不 await,不抛。

## 4. 设置

- [x] `AppSettings` 多一个 `soundOn`,持久化到已有的 `PreferencesStore`。
- [x] 设置页一个 `SwitchListTile`,复用 `header.sound.*`。**零新增键。**

## 5. 接到事件点上

- [x] 落子 / 吃子(象棋)/ 被催 / 赢 / 输 / 和。
- [x] 吃子只有象棋会触发 —— **由棋盘 renderer 说了算**,不是按棋种写死。

## 6. 判据

- [x] 单测:每个事件都产出非空、非全零样本。
- [x] 单测:`move-place` 的**主频**落在容差内(过零率或 Goertzel)。
      **这是这一笔的招牌判据:我听不见,但它是数字。**
- [x] 单测:峰值不削顶(不等于 32767)。
- [x] 单测:关掉不调播放层(**配前置断言**证明开着时会调)。
- [x] 单测:播放层抛异常时落子照常完成、无错误文案。
- [x] 单测:重启后记得开关。
- [x] **正面对照:让合成器忽略频率参数,看主频那条红。**
- [x] **正面对照:静音时改成「音量置零照播」,看那条红。**

## 7. 不回归

- [x] `flutter analyze` 零问题;`flutter test` 全绿;`shared_sync_test` 绿(零新增键)。
- [x] 既有集成测试逐个跑。
- [x] **Android 真机构建 + 安装必须成功** —— 插件的原生部分只有这一步能证。

## 8. 收尾

- [x] `JOURNAL.md` 一条;CLAUDE.md 手机端那节一行。
- [x] 开 PR,写明**「听没听见」不在判据里**,要真机确认。
- [ ] **没做:实际听一遍。** APK 已装到真机上,插件在真机启动时没有报错 —— 但
      「有没有声音出来」只有你能确认,而这一笔的所有判据都到不了那一步。
