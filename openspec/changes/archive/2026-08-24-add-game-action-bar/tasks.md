# tasks — add-game-action-bar

## 1. 组件

- [x] 新增 `pages/rooms/room-page/action-bar/action-bar.{ts,html,spec.ts}` —— 纯展示 + 事件外抛,输入 `state` / `mySeat` / `turnRemainingMs` / `canUrge`,输出 `resign` / `leave` / `urge`。**110 行**。
- [x] 辞局的 CDK Dialog 跟着按钮一起搬(`ResignConfirmDialog`)—— 没有手搓确认框。
- [x] 回合指示与倒计时的算法照搬侧栏的:两座位说颜色、座位数 > 2 说座位号,`≤ 10s` 加 `text-danger`,`≤ 0` 显示 `0:00`。
- [x] `RoomPage` 在棋盘那一列的**最后**渲染它,`w-full`。

## 2. 侧栏

- [x] 删掉回合指示、倒计时、按钮组、观众分支(模板 −54 行)。
- [x] `sidebar.ts` 跟着删干净:`Dialog`、`FIRST_SEAT`、三个输入、三个输出、`isPlayer` / `myTurn` / `countdownText` / `countdownDanger` 与三个方法。**逐项断言删除命中**,而不是删完看一眼 —— 留下一个没人读的 `@Input` 就是下一个人的困惑来源。110 → 51 行。
- [x] `sidebar.spec.ts` 的 host 组件跟着只剩 `[state]`。

## 3. 视觉与触达

- [x] `panel` 角色,**没有新增 utility、没有新增 token**。
- [x] 每个按钮 `min-h-11`;375 px 下操作条换行成两行(高 118),**无横向溢出**(`scrollWidth == clientWidth == 375`)。
- [x] 倒计时是 `well` 里的等宽数字。它身上**没有静态 `text-*`** —— `well` 自己给前景色,所以「低于阈值变红」这个状态里只有一个候选前景色。这是 `fix-primary-label-contrast` 那条新规则要求的形状。
- [x] 零新增 i18n 键 —— 由既有的双语 key 集合相等测试自证。

## 4. 测试(+12,搬走 4,页面级 +2)

- [x] 回合指示两支都有样本:三座位说「座位 3」**且不说**「白方走棋」;两座位说「黑方走棋」**且不说**「座位 1」。
- [x] 倒计时:`M:SS` 格式;30s 时**没有** `text-danger`,5s 时有 —— 两头都断言,否则一个恒红的实现也是绿的。
- [x] 观众:先断言**玩家视角下三个按钮确实画出来了**(前置条件),再断言观众看不到 —— 而观众仍然看得到回合指示。
- [x] 辞局:先开 dialog 才发;dialog 被取消时**一次都不发**。
- [x] 催一下:`canUrge` 为假时 `disabled`,为真时可点且发出事件。
- [x] 按钮走查:`querySelectorAll('button')` ≥ 3(前置条件:样本非空),每个都带 `min-h-11`。
- [x] 页面级(两个组件都挂着):三个按钮与倒计时**在操作条里、不在 `aside` 里**,而侧栏仍然有座位名单 —— **两半都断言**,只断言侧栏没有的话,一个把它们整个删掉的实现同样是绿的。
- [x] 页面级:操作条是棋盘那一列的**最后一个孩子**。

## 5. 变异(先看到红才算数)

- [x] 操作条不再是棋盘列的最后一个 → 红。
- [x] 按钮去掉 `min-h-11` → 红。
- [x] 丢掉 `isPlayer()` 判断,观众也拿到按钮 → 红。
- [x] 倒计时阈值 10s → 0 → 红。
- [x] 辞局跳过 dialog 直接发 → 红。
- [x] `seatCount` 退化成 `seats.length`(**历史上真出过的那个缺陷**)→ 红,点名「说哪个座位」那条。
- [x] 第一版第六条写的是模板里 `@if (false)`,而那**不是变异**:Angular 模板编译直接报错,「exit 1 且没跑测试」读起来和 kill 一模一样。换成上面那条。

## 6. 量出来的东西

- [x] 375 px 纵向堆叠,**同一个文件、同一次运行、搬前搬后并排**:

  | | 搬之前 | 搬之后 |
  | --- | --- | --- |
  | 棋盘 | 311×311 @ 100 | 同 |
  | 倒计时 | **638**(高 17) | **440**(高 36) |
  | 催 / 认输 / 离开 | **675 / 713 / 751**,各 **30** | 都在 **488**,各 **44** |
  | 最下沿 | **781** | **532** |

  一台 375×812 的手机可用高度约 700 → 搬之前**认输(713)与离开(751)在屏幕外**,倒计时贴在最下沿。
- [x] **第一版量错过一次:** 搬前搬后各一个探针文件,两边棋盘宽不一致(295 对 311)—— 一份留着 `<main>` 上的 `sm:px-6 lg:px-8`,而那两个变体在 pane 的 **981 视口**下是匹配的,即使容器只有 375。**两个环境里的数不是一个对比**,所以重做成一个文件两半。
- [x] 按钮实测 **44 px**(jsdom 无布局,类名断言只能证明写了)。
- [x] 初始包 476.75 → **476.84 kB(+0.09)**。组件本身进的是懒加载的 `room-page` 块(54.25 kB);**那 0.09 是新 CSS utility**(`min-h-11` / `min-h-9` / `tabular-nums`),不是组件。预期写的是「0 变化」,而实际不是 0 —— 记下来。
- [x] `npm run lint` 0(1204 次对比度读数)/ `test:ci` **896 绿** / 两个 tsconfig 0 / `build` 0。

## 7. 没做的

- [x] 吸底(`position: sticky`)—— 量到不需要,理由记在 spec 里。
- [x] 牌桌那一排出牌按钮不搬,理由记在 spec 里。
- [ ] **真人对局的浏览器端到端没做。** 登录链路上次没走通,而这一版的判据是布局与顺序,两者都在探针和页面级测试里量过 —— 但「一局真棋里点认输」没有跑过。
