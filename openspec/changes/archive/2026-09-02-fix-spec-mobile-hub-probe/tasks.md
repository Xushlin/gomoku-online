# tasks

## 1. 让探针真的证明入向

- [x] `test/hub_probe_test.dart`:在 `JoinRoom` 之前订阅 `RoomState`,落子之后**等一条推送到达**。
- [x] 判据是**内容**,不是「连上了」:收到的那条 payload 里 MUST 有那一步棋。
      「连接没断」「回调注册成功」都不算 —— 那正是这次的缝。
- [x] 等待要有**超时**并在超时时失败,MUST NOT 无限等。一个永远挂着的测试与一个通过的测试
      在 CI 上不一样,但在人眼里一样容易被忽略。
- [x] 把那条自称「正面对照」的 REST 断言的注释改准:**它证明的是服务端收到了,不是客户端收得到。**
      两件事,而混在一起正是这个缝的形状。它可以留着(它仍然有用),但不能再冒充入向的证明。
- [x] **正面对照:把订阅名改成 `RoomStateChanged`,看探针红** —— 而不是绿着跑完。
      这一条 MUST 亲手跑一次:它就是这次要防的那个 bug。

## 2. 把已经落地的走查写进 spec

- [x] `hub_contract_test.dart` 已经在 `main` 上,本笔只补要求(spec delta 已写)。
- [x] 确认那条走查的两个非空断言还在(服务端 ≥ 8 个名字、客户端非空)——
      **确认,别假设**;spec 里那条 Scenario 就是钉它的。

## 3. 走查与不回归

- [x] `flutter analyze` 零问题;`flutter test` 全绿(探针在没有后端时仍然 skip,而 skip 要说明理由)。
- [x] 起一个后端,**真的跑一次探针**并把输出贴进 PR —— 一个只在 skip 状态下「通过」的探针
      正是这条要求要防的东西。

## 4. 收尾

- [x] `JOURNAL.md` 一条。
- [x] `CLAUDE.md` **不改** —— 那条教训在修 bug 那一笔里已经写进去了,再写一遍就是两份副本。
- [x] 归档:`changes/` 下只有这一笔,没有顺序问题。
