# tasks

## 1. 模型与判定

- [x] `GameSnapshot` 解析 `result` / `winnerUserId` / `endReason`(都可空 —— 进行中的对局没有)。
- [x] `GameViewModel.outcome`:标题键 + 原因键,或 null。赢输**按 id 比**,不按用户名。
- [x] `GameResult` 只有 `Ongoing=0` / `Decided=1` / `Draw=3`(**没有 2**)—— 按名字解析,不按序号。

## 2. 界面

- [x] 结束时弹一个对话框,两条出路:`game.ended.back-to-lobby` / `game.ended.dismiss`。
      **`dismiss` 那个键本身就说明了形状**:关掉之后还能看棋盘,所以是对话框不是常驻横幅。
- [x] 关掉之后 MUST NOT 再弹 —— 每条推送都重弹一次是比不弹更烦的缺陷。
- [x] 用 `showDialog`,不手搭覆盖层。

## 3. 删掉那条没人消费的流

- [x] 删 `GameEnded` 订阅与 `_errors`。**确认没有别的消费者**(写这条时是 0 个)。
- [x] `hub_contract_test` 订阅数 3 → 2,走查应仍然绿 —— **确认,别假设**。

## 4. 判据

- [x] 单测:赢 / 输 / 和 / 未结束**四个方向**;三种结束原因;关掉之后不再弹。
- [x] 单测:每一个会用到的 `game.ended.*` 键在两个 locale 里都有文案。
- [x] 集成测试:**下完一整局人机对局**(一字棋 Easy 最快),断言**屏幕上**出现结果。
      判据是屏幕不是服务端。
- [x] **正面对照:把赢输判定反过来,看那三个方向里对应的红。**
- [x] **正面对照:把 `result` 解析删掉,看「结束时说结果」那条红。**

## 5. 走查与不回归

- [x] `flutter analyze` 零问题;`flutter test` 全绿;既有集成测试**匹配器与期望值逐字未变**。
- [x] 装到那台真机上(已装,等用户下一局确认)—— 这一条缺陷就是真机发现的。

## 6. 收尾

- [x] `JOURNAL.md` 一条。
- [x] `CLAUDE.md`:手机端那节加**一行**(结果只有一个来源:房间快照)。
- [x] PR 里报净改动行数。
