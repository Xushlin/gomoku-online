# tasks — restyle-klotski-board

## 1. 先让「三个皮肤一模一样」有一条会红的检查

- [x] 检查落在 `check-styles.mjs` 而不是 vitest —— 这个仓库的测试构建读不到 CSS 文本(既有要求里写着,是量出来的)。它比的是**值**:同一个变量在任意两个皮肤下不许相同。
- [x] **反面对照做过了:** 判据若写成「引用了 `var(--board-…)`」,在**今天这个坏版本上也是绿的**(它确实引用了 `--radius-card`)。所以判据只能是值。
- [x] 变异:让 midnight 的托盘取 wood 的值 → 红,并点名是哪两个皮肤、哪个变量。

## 2. 接进皮肤层

- [x] 三个皮肤各加 17 个 `--kt-*`(托盘、四类棋子的面与字、出口、落点、选中环)。皮肤变量集 **26 → 43**。
- [x] 变异:midnight 少一个 `--kt-guard-text` → 红并点名。既有机制,不靠谁记得。
- [x] `.kt-*` 不再消费 `--color-surface` / `--color-text`。

## 3. 角色从尺寸推

- [x] `roleOf(piece)` 是纯函数,**全的**:`target` → 主帅;`1×1` → 兵;`height > width` → 竖将;其余 → 横将。没有第五类兜底,所以不用为一个假想关卡付一份死 token。
- [x] 单元测试:四类各一条、总性一条(五种奇怪形状都落得进去)、不看名字一条、`target` 优先一条。
- [x] 渲染测试:**先断言样本四类齐全**(这是下面每一条的前提),再断言每个棋子恰好一个角色类。
- [x] 变异:把 `roleOf` 改成「除了主帅都是竖将」→ 3 条红。

## 4. 让它真的滑

- [x] 定位从 `grid-area` 换成 `transform: translate()`;步长用 `cqw`,**不是 `%`**(百分比在 `translate()` 里按元素自己解析 —— 牌桌栽过四次)。
- [x] 坐标系放在**子元素** `.kt-grid` 上:一个元素用不了自己的容器查询单位。第一版写的 `@container` 查自己,是死代码,已删。
- [x] 测试:移动**恰好一个**棋子的 `--kt-r` / `--kt-c` 变;没有任何棋子声明 `grid-area`。
- [x] 变异:把 `grid-area` 加回去 → 红。
- [x] 删掉 `global.css` 里那句「browsers animate as a layout change」。
- [x] `prefers-reduced-motion: reduce` 下 `transition-duration: 0s`。

## 5. 尺寸

- [x] 写死的 `max-width: 360px` 去掉;页面容器 `lg:max-w-xl`。桌面上棋盘 **360×450 → 514×643**。
- [x] 格子恒为正方形:行高由 `--kt-cell` 算,不靠 `aspect-ratio`(带 padding 的 `aspect-ratio` 算在边框盒上,内容盒会差出两倍 padding)。
- [x] 375px:**在该宽度下重新渲染**后量,`scrollWidth == clientWidth`,棋子最右 323.04 ≤ 盘宽 324.5。

## 6. 对比度

- [x] 新增一根轴:皮肤 × 主题 × 明暗 × 角色 = **224 条读数**(此前 board-skins 那一层一条也没量过)。
- [x] 让检查能解 `color-mix()` —— 否则跟随主题那个皮肤的每一条都会被**静默跳过**。
- [x] 变异:兵的字画成和面同色 → 红(1.10:1);跟随主题的皮肤把主帅面调暗 → 红,逐主题逐明暗点名。
- [x] **正面对照第一版太弱,是变异发现的:** 写的是「读数 ≥ 皮肤×主题×明暗×角色」,而打断 `color-mix()` 解析后读数 224 → 200,**照样通过**。改成按「每个组合都必须量到过」之后,同一个变异报「96 个组合里 24 个被静默跳过」。

## 7. 名单不许手写

- [x] 角色名单从 `model.ts` 的 `KlotskiRole` 联合类型推导 —— 它第一次在检查脚本里就是手写的 `['boss','general','guard','soldier']`,而「一份手写名单冒充注册表」这个仓库修过六次。
- [x] 顺带钉住:每个角色都要有 `.kt-piece--<role>` 规则与 `--kt-<role>-face` / `-text`。
- [x] 变异:联合类型里加第五类 → 4 条红(缺 CSS 规则、缺两个 token、24 个组合没量到)。

## 8. 在真浏览器里看

- [x] 三个皮肤:托盘与四类棋子的面**两两不同**(此前逐字节相同)。
- [x] **而这一趟抓到一个只有浏览器看得见的缺陷:** 跟随主题的皮肤下棋子 `background-image` 是 `none` —— 面是一个颜色,而颜色不是合法的 `background-image` 值。改成 `background` 简写,并加了一条规则钉住它。
- [x] 明暗:wood 的四类面与字在 `.dark` 下全部换过一套。
- [x] 走一步棋:坐标 3,3 → 2,3,落点标记的尺寸是 **1×2**(跟着选中的棋子,不是一个格子)。
- [x] 棋子的实际像素位置与格坐标逐个吻合(误差 0.02 格 = 半个间隙)。
- [x] **量出一条要记的坑:** 面板不合成时**布局值会更新、`transform` 不会**。JS 改容器宽度后棋子宽度跟着新格距变、位置停在旧格距上 —— 读起来和真溢出一模一样。重新渲染后再量才是真的。

## 9. 收尾

- [x] `npm run lint` 0 / `test:ci` **931 绿**(+10)/ 两个 tsconfig 0。
- [x] `web-board-skins` 那条漂移(要求已被删掉的 `core/theme/skins/*.ts`)一并改掉。
