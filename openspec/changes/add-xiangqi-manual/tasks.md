# tasks — add-xiangqi-manual

## 1. 先把 31 局全部过一遍规则,再写任何界面

- [ ] 一次性抓下 31 条记录(id `64433`–`64463`),解析 `DhtmlXQ` 块,存成 `backend/data/manuals/xiangqi-meihuapu.json`。之后**不再联网**。
- [ ] 写一个校验:每条记录的 `binit` MUST 等于标准摆法(第 1 局逐对比过了,剩下 30 条没有);**不等就拒绝并报出是哪一条** —— 若有残局式的起始局面,那是另一件事,v1 不做。
- [ ] 把每条的每一手喂 `XiangqiRules`,红先交替。**先看到红**:故意把某一手的四位数字改坏,校验必须报出「第几手 + 原招法 + 规则的理由」。
- [ ] **正面对照**:校验器 MUST 在「招法串为空」和「长度不是 4 的倍数」时报错,而不是当成一条 0 手的合法棋谱。第 1 局那次探针里,`Decided` 出现在第 46 手 —— 记下每条的终局手数,一条中途就 `Decided` 的记录说明数据或解码有问题。
- [ ] 报告:31 条里多少条全程合法、多少条被拒、被拒的是第几手。**这一步的结果决定后面还做不做。**

## 2. 后端

- [ ] `XiangqiManual` / `XiangqiManualLine` 两张表 + 配置 + 一个新迁移(guarded)。
- [ ] `XiangqiManualSeeder`,照 `PuzzleLevelSeeder` 的形状;幂等。
- [ ] `GET /api/manuals/xiangqi`(目录,按局分组)与 `GET /api/manuals/xiangqi/{lineId}`(单条),**允许匿名**。
- [ ] 招法用 `MoveDto` 同形返回 —— 不新造招法表示。
- [ ] 断言:播种前后 `Rooms` 行数不变、任意用户 rating 与 `gamesPlayed` 不变、`GET /api/users/{id}/games` 不含棋谱。**这三条是「不借用 Room」这条要求唯一会红的地方。**

## 3. 把 scrubber 抽出来(唯一触碰既有代码的地方)

- [ ] 从 `ReplayPage` 抽出纯展示的 scrubber 组件:输入 `totalMoves` / `currentPly`,输出「跳到第 N 手」;计时留在组件内,当前半手的真源留在页面。
- [ ] **回放页既有的 scrubber 断言一条不改** —— 它们是「抽取没有改行为」的可执行形式。
- [ ] 变异:把组件里的边界判断改坏 → 回放页那几条必须红。
- [ ] 一条断言证明两个页面用的是同一个组件(两个模板里都不许各自出现 `type="range"`)。

## 4. 前端两个页面

- [ ] 目录页:按局分组(8 局),局内列变化;加载 / 空 / 错误三态都有真实 UI。
- [ ] 学习页:`XiangqiBoard` 只读 + 共享 scrubber + 注解区。
- [ ] 注解区高度稳定:从有注解走到无注解,高度 MUST 不变(否则每走一步棋盘上下弹)。
- [ ] i18n:`manual.*` 键 zh-CN / en 齐备;模板里不许硬编码显示串。
- [ ] 375px:用**标题最长**的那一条测无横向溢出。
- [ ] 未登录访问两个页面都不跳登录。

## 5. 在真浏览器里看

- [ ] 冷启动 → 目录 → 进一条谱 → 播放到底 → 确认棋子在动、注解跟着走。
- [ ] 三个棋盘皮肤 × 明暗各看一眼(象棋棋盘已经接了皮肤层,这里只是确认没漏)。
- [ ] **面板不合成时 DOM 读数是陈旧的**,交互后要 `window.ng.applyChanges(...)` 再读。
- [ ] 375px 与桌面各一遍。

## 6. 收尾

- [ ] `dotnet build` / `dotnet test` / `npm run lint` / `npm run test:ci` 全绿。
- [ ] 初始包:两个新页面必须是惰性 chunk,**用构建归因证明**,不是「它写了 loadComponent 所以是懒的」。
- [ ] 数据文件里记下来源与那条版权判断(招法与原始局名可用,近人白话注解不导入)。
