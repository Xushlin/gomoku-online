# tasks — add-xiangqi-manual

## 1. 先把 31 局全部过一遍规则,再写任何界面 —— **已跑完**

- [x] 一次性抓下 31 条记录(id `64433`–`64463`),解析 `DhtmlXQ` 块,存成 `backend/data/manuals/xiangqi-meihuapu.json`。之后**不再联网**。
- [x] 已验:`binit` **31/31** 等于标准摆法。写成校验(每条记录的 `binit` MUST 等于标准摆法(第 1 局逐对比过了,剩下 30 条没有);**不等就拒绝并报出是哪一条** —— 若有残局式的起始局面,那是另一件事,v1 不做。
- [x] 已跑:**1391 个半手全部合法,rejected=0**。写成校验(每条的每一手喂 `XiangqiRules`,红先交替。**先看到红**:故意把某一手的四位数字改坏,校验必须报出「第几手 + 原招法 + 规则的理由」。
- [x] **正面对照已成立**:`decidedEarly=0`。仍需写进校验器:校验器 MUST 在「招法串为空」和「长度不是 4 的倍数」时报错,而不是当成一条 0 手的合法棋谱。第 1 局那次探针里,`Decided` 出现在第 46 手 —— 记下每条的终局手数,一条中途就 `Decided` 的记录说明数据或解码有问题。
- [x] 报告已出:31/31 全程合法;11 局以杀棋收,20 局未终局 —— 所以校验器 MUST NOT 要求终局(原文:31 条里多少条全程合法、多少条被拒、被拒的是第几手。**这一步的结果决定后面还做不做。**

## 2. 后端

- [x] `XiangqiManual` / `XiangqiManualLine` 两张表 + 配置 + 一个新迁移(guarded)。
- [x] `XiangqiManualSeeder`,照 `PuzzleLevelSeeder` 的形状;幂等。
- [x] `GET /api/manuals/xiangqi`(目录,按局分组)与 `GET /api/manuals/xiangqi/{lineId}`(单条),**允许匿名**。
- [x] 招法用 `MoveDto` 同形返回 —— 不新造招法表示。
- [x] 断言:播种前后 `Rooms` 行数不变、任意用户 rating 与 `gamesPlayed` 不变、`GET /api/users/{id}/games` 不含棋谱。**这三条是「不借用 Room」这条要求唯一会红的地方。**

## 3. 把 scrubber 抽出来(唯一触碰既有代码的地方)

- [x] 从 `ReplayPage` 抽出纯展示的 scrubber 组件:输入 `totalMoves` / `currentPly`,输出「跳到第 N 手」;计时留在组件内,当前半手的真源留在页面。
- [x] **回放页既有的 scrubber 断言一条不改** —— 它们是「抽取没有改行为」的可执行形式。
- [x] 变异:把组件里的边界判断改坏 → 回放页那几条必须红。
- [x] 一条断言证明两个页面用的是同一个组件(两个模板里都不许各自出现 `type="range"`)。

## 4. 前端两个页面

- [x] 目录页:按局分组(8 局),局内列变化;加载 / 空 / 错误三态都有真实 UI。
- [x] 学习页:`XiangqiBoard` 只读 + 共享 scrubber + 注解区。
- [x] 注解区高度稳定:从有注解走到无注解,高度 MUST 不变(否则每走一步棋盘上下弹)。
- [x] i18n:`manual.*` 键 zh-CN / en 齐备;模板里不许硬编码显示串。
- [x] 375px:用**标题最长**的那一条测无横向溢出。
- [x] 未登录访问两个页面都不跳登录。

## 5. 在真浏览器里看

- [x] 冷启动、**未登录** → 目录页 8 局 31 条、无原始 i18n 键漏出。
- [x] 点进第 1 局:90 个点、32 个子、一个 scrubber、一个进度条、`0 / 46`。
- [x] 第 1 手:相 从 (9,6) 走到 (7,4) —— **子真的在动**。末手 23 个子、`46 / 46`、⏭ 禁用、按钮变 Replay;回到第 0 手棋盘完整重建成 32 子。
- [x] 边界禁用在真浏览器里也对(第 0 手时 ⏮/⏪ 禁用)。
- [x] 谱评那句是「The manual judges: Black is better」,**页面上不含任何终局类词**。
- [x] 自动播放**连续推进**:0 → 1 → 2 → 3 → 4 → 5。
- [x] 大厅入口:象棋有、五子棋没有 —— manifest 驱动,两个方向都看过。
- [ ] **375 px 的像素测量没做到。** 面板全程隐藏,`innerWidth` 与 `clientWidth` 都是 0,布局读数不可用。改成钉机制:栅格在 `sm` 以下是一列、最长局名带 `break-words`(jsdom 没有布局引擎,`scrollWidth` 是 0,本仓库既有的 375 px 断言同理)。**这一条是欠着的,不是过了。**

### 而自动播放那一条,第一次量出来是「坏的」

面板不合成时零区变更检测不跑,于是 `seek` 发出去、页面写了信号,**输入回不到子组件** —— 计数器停在 `1 / 46` 再也不动。那个样子**和「自动播放坏了」一模一样**。在等待期间手动跑变更检测之后,它是 0→1→2→3→4→5。

## 6. 收尾

- [x] `dotnet build` / `dotnet test` / `npm run lint` / `npm run test:ci` 全绿。
- [x] 初始包:两个新页面必须是惰性 chunk,**用构建归因证明**,不是「它写了 loadComponent 所以是懒的」。
- [x] 数据文件里记下来源与那条版权判断(招法与原始局名可用,近人白话注解不导入)。
