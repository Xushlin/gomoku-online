# tasks — play-from-position

## 0. 顺序

- [ ] `add-xiangqi-endgames` 的归档(#149)合并。**这一条不是礼节** —— 本变更要 `MODIFIED` 的东西目前还不在 live spec 里,而手工合并两个未归档变更正是无关句子被静默回退的方式。
- [ ] 两份 delta 现在都是 `ADDED`,所以归档后 `validate --strict` 应当直接过;过不了就说明有东西漂了,先查再动代码。

## 1. 先把内核那条不变量改对,再碰象棋

- [ ] 新接口 `IPositionalStartRules`,与 `IDealtGameRules` **并列**:设置由调用方选定、由规则校验。
- [ ] `Room` 的 `needsSetup` 改成「两者之一」。**两个方向的抛都保留** —— 变异:把「不要设置却给了」那条删掉,必须有测试红。
- [ ] 校验发生在**存下来之前**:变异 —— 让校验在 `new Game(...)` 之后跑,「房间留在 Waiting」那条必须红。
- [ ] 一条遍历注册表的走查:`IPositionalStartRules ⇒ !IsRated`,与既有 `IsRated ⇒ SeatCount == 2` 并排。
- [ ] **两种设置来源都要在样本里** —— 否则「两种来源」在单一种类上恒真。

## 2. 象棋:抽取,而不是复制

- [ ] 把走子合法性与胜负判定抽成共用的一份;`XiangqiRules` 的**对外行为不变**。
- [ ] **既有象棋测试一条不改地通过** —— 那是「抽取没有改行为」的可执行形式。而这句话上一次说过头过两次(`lazy-header-menu`、`add-xiangqi-manual`),所以先跑一遍再写进规格:改不了的话,如实写「断言不动,夹具变了」。
- [ ] `XiangqiEndgameRules`:`IPositionalStartRules` + `IFirstSeatRules`,`IsRated => false`,走子委托给共用那份。
- [ ] 一条断言:同一个局面 + 同一步棋,两个棋种判断一致,而样本用**两条路径都能到达**的局面。

## 3. 设置的形状与校验

- [ ] 设置编码起始局面(90 字符盘面串)与先走方 —— 与古谱那两列同一个表示,**不新造第二种**。
- [ ] 校验:长度、恰好一帅一将、将/士在九宫、相/象在各自的点、先走方在 `[0, SeatCount)`。
- [ ] **每一条都要先看到红**,而且报出的是**哪一条**不满足。
- [ ] 变异:让校验失败时静默退回标准开局 → 必须有测试红(那种坏表现成「这局怎么是开局」)。

## 4. 建房带线路 id

- [ ] `CreateRoomCommand` 多一个可选的线路 id;服务端据它取起始局面与先走方。
- [ ] **请求里不许有盘面串** —— 一条源码规则或 DTO 形状钉住它。
- [ ] 两个方向都校验:键与 id 必须同时给或同时不给。变异各一条。
- [ ] 不存在的线路 → 清楚的拒绝,而不是开一局标准开局的棋。

## 5. 那条**故意**会红的

- [ ] `The_unrated_games_are_tictactoe_doudizhu_and_wakeng` 会红。**这是设计好的信号**,不是要绕开的障碍。
- [ ] 改它的时候把**每个成员不计分的理由**写进去:一字棋是「必和」,残局是「开局就不公平」。两条理由不同,正是「恰好」在第二个同类出现时要问的问题。

## 6. 前端

- [ ] 房间页与大厅按「象棋族」挑棋盘,而不是单一键 —— 现在是 `gameKey === XIANGQI_KEY`。
- [ ] 学习页多一个「摆此局对弈」入口。**它 MUST NOT 出现在那条「不许有接着自己走的入口」的源码规则之下**:那条规则守的是「平台不判对错」,而这里是开一局正常的、由人下的棋 —— 所以规则要收窄成「不许判定」,并把这次的区别写进注释。
- [ ] 残局房界面写明**平台不判和棋**;i18n 两份 locale 齐备。
- [ ] 375 px。**先读 `innerWidth` 再决定量不量得到**(上一趟是 0,这一趟是 375)。

## 7. 在真浏览器里看

- [ ] 开一局黑先走的残局:黑真的先动,红此时点不动。
- [ ] 一步在标准开局下合法、在这个残局下非法的走子被拒。
- [ ] 认输能用(两座位),而这一局**不进战绩、不动 ELO**。
- [ ] 那句「平台不判和棋」在界面上看得见。

## 8. 收尾

- [ ] 后端 + 前端全绿,lint 0,build 0;初始包与归因。
- [ ] `CLAUDE.md`:「走法合法性判据欠着」那条延期项**可以收紧一半** —— 残局能从局面开局之后,导入校验能不能改走强路径,要重新量一次再说,**而不是假定可以**。
