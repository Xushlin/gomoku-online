# Tasks — add-doudizhu

## 1. 一步棋的编码

- [x] 1.1 `DoudizhuMove` —— `bid:N` / `pass` / `play:<cards>`
- [x] 1.2 标签是可读英文,不是单字符前缀
- [x] 1.3 `Parse` 拒绝一切认不出来的文本
- [x] 1.4 一手 20 张装进 `Move.Text` 的 64 字符(实测 25)

## 2. 局面重建

- [x] 2.1 `DoudizhuTable.Reconstruct(MatchState)` —— 阶段 / 地主 / 底分 / 三家手牌 / 桌面 / 连过数
- [x] 2.2 重放历史时不再校验(它们当初就是这么被接受的),与 `XiangqiRules.Replay` 一致
- [x] 2.3 地主拿底牌:17 + 3 = 20,并重新排序

## 3. 规则

- [x] 3.1 `DoudizhuRules : IGameRules, IDealtGameRules, ITimeoutFallbackRules`
- [x] 3.2 叫分:严格加价、叫 3 立即结束、三家不叫流局
- [x] 3.3 出牌:持有校验、牌型识别、压牌、首出不能过、两家过牌清桌
- [x] 3.4 叫分结束用 `MoveApplication.NextSeat` 把出手权交给地主
- [x] 3.5 超时兜底:叫分不叫;出牌能过就过,首出出最小的单牌
- [x] 3.6 `IsRated = false`,`SupportsHumanVsHuman = true`,`SeatCount = 3`

## 4. 登记

- [x] 4.1 `GameKeys.Doudizhu`
- [x] 4.2 `BuiltInGameRules.All` 一行 —— **DI 不用改**,它早就从这份清单派生
- [x] 4.3 五条遍历注册表的不变量**一个断言都没改**就覆盖到了新棋种

## 5. 断言

- [x] 5.1 编码的往返与拒绝
- [x] 5.2 叫分的每一条分支
- [x] 5.3 出牌 / 过牌 / 清桌 / 获胜
- [x] 5.4 兜底的三种情形,外加一条"每一种兜底动作都合法"
- [x] 5.5 一整局走通真 `Room`(20 手出完 + 38 次过牌)
- [x] 5.6 源码级:聚合不提这个棋种;抽象层只多一个 `GameKeys` 常量

## 6. 验证

- [x] 6.1 `dotnet test Gewu.slnx` 全绿 —— **1266**(Domain 855 / Application 286 / Infrastructure 125)
- [x] 6.2 `git status backend/src` 只有两个内核相邻文件,`+4 / -1`;`Rooms/` 零改动
- [x] 6.3 前端零改动;无迁移
- [x] 6.4 `openspec validate --strict` 通过,且每个 MODIFIED 标题**去 live spec 核对过存在**
- [x] 6.5 `AiSmoke` 对着真服务器 **35 条全绿**(此前 22 过 1 败,见 §8)

## 7. 实现记录

### 五条"斗地主落地那天会红"的断言,全都红了 —— 而那是它们的用途

内核那六个变更里,我写了五条形如「今天还没有棋种如何如何」的断言,每条都带一句"斗地主落地那天
这条会红,那时该重新想"。这次它们**全部**变红:

| 断言 | 处理 |
| --- | --- |
| `Every_registered_game_has_two_seats_today` | 按它自己注释里写的改成"每个**计分**棋种两个座位",并**另加一条**"至少有一个棋种超过两个座位" —— 否则删掉斗地主之后前者会以空循环继续绿着 |
| `No_built_in_game_deals_a_setup_yet` | → "**恰好一个**",且是 doudizhu |
| `No_built_in_game_falls_back_on_timeout_yet` | → "**恰好一个**",且是 doudizhu |
| `TicTacToe_is_the_only_unrated_versus_game` | → 两个不计分棋种,而**两者的理由不同**,分别写进注释 |
| `Rolling_back_needs_no_guard_and_this_is_why` | 见下 —— 它守的那个**理由本身**失效了 |

"恰好一个"比"至少一个"有牙:第二个棋种要设置 / 要兜底的那天它会红,而那正是该问"这两个棋种的
需求真是同一种东西吗"的时刻。

### 那条迁移断言守的是一个**已经失效的理由**

`AddGameSetup` 的 `Down` 直接删列,而当时的论证是:「`Setup` 的唯一读者是需要它的那个棋种的规则,
而回滚到本迁移之前意味着那个棋种还不存在 —— 所以不可能有非 `NULL` 的行需要保护」。

斗地主落地之后这句话不成立了。重新想的结论:**那个 `Down` 确实会毁数据**,而且比"回滚到一个没有
斗地主的构建"更难看 —— 回滚再前滚一次,列会以全 `NULL` 回来,房间还在、还像能玩,而规则在下一手
抛「This doudizhu game has no deal recorded」。

那个迁移**不能就地改**(已合并的迁移不改是硬规矩),所以处理是:把后果**演出来**并留在案上
(`Rolling_back_across_this_migration_destroys_a_deal`),外加一条盯**范围**的断言(今天只有一个棋种
会写非 `NULL` 的 `Setup`,所以"加一个带守卫的新迁移"那笔账只涉及一个棋种)。在没有部署、库随时
可删的今天,那笔钱不值得花;第二个棋种要设置的那天这笔账变大,该重新估。

### 两条真缺陷,都是测试逮到的

**一、`FormatException` 漏出去了。** `Card.DecodeMany` 对"不认识的字符"与"同一张牌两次"都抛
`FormatException`,而那不是 `DomainException` —— 于是一个畸形载荷(`play:!!!`、`play:AA`)会以未映射
异常冒出去变成 **500**,客户端看到"服务器出错了",而实际上是它自己发错了。`Parse` 现在把它转成
`InvalidMoveException`。

**二、我自己写的重复牌检查是死代码。** `DecodeMany` 早就在拦(它自己的注释就写着"早在这里拦下,
规则层就不必再想"),所以 `Parse` 里那条永远到不了。**与 `add-doudizhu-cards` 里的 `WingsAreLegal`
是同一个缺陷**,发现方式也一样:为它写的那条测试拿到了别的异常。

### 一条我写错的**理由**(结论对的)

`DoudizhuMove` 的类注释与第一版测试都说「一个裸的 `"pass"` 就是一手四张牌的合法编码」,用来论证
标签的必要性。**结论对,理由错**:`"pass"` 里有两个 `s`,而 `DecodeMany` 恰好拒绝重复的牌,所以那
一串会抛。也就是说 `"pass"` 的安全**是运气**(那个词刚好有重复字母),不是标签的功劳。

换成 `paw` / `cab` 这种没有重复字母的词,歧义就真的在那里 —— 测试现在用 `cab`,并把 `"pass"` 会抛
这件事也断言下来,免得下一个人以为那是设计。**一个正确的结论配一个错的理由,下一个人照那个理由
推下去就会走错。**

### 验收标准的第一版**比这个仓库的实际做法更强**

我写的是「`Rooms/` 与 `Games/Abstractions/` 下都不许提到这个棋种」,写成断言之后当场红在
`IGameRules.cs` 上 —— 因为 `GameKeys` 就住在那个文件里,而**五个棋种的键全在那儿**。也就是说那条
标准与 `add-xiangqi` 以来的每一次都矛盾,只是此前没人把它写成断言。

**一条从来没被执行过的验收标准,可以和实际做法矛盾很多年。**

诚实的标准分两半,现在各有一条断言:聚合(`Rooms/`)一个字都不许提;抽象层只许在 `GameKeys` 里
多一行常量。两条都先断言扫到的文件集合非空 —— 路径写错时必须失败,而不是以"没有反例"通过。
git 侧也核对了:`backend/src` 只有那两个文件动过,`+4 / -1`。

### 那一整局是怎么脚本化的

发牌是固定种子,但"谁能压谁"取决于牌 —— 所以脚本不去依赖它。座位 0 叫 3 分当地主,然后**一张一张
出单牌**,两名农民每次都过牌。它只依赖两条与牌无关的性质:**单牌永远是合法牌型**,以及**两家过牌
之后桌面清空、轮回打出那一手的人**。20 手出牌 + 38 次过牌 + 1 次叫分 = 59 步,全部走真聚合。

### 两件明确留下的东西

**一、`DoudizhuScoring.Settle` 仍然没有生产调用方。** 它算三个座位各得多少分,而那需要一处存分的
地方 —— 一条按分的榜。触发条件:那条榜落地。与 `add-idiom-dictionary` 的 `FindByWordAsync` 同形状,
而那次的教训是**端口挑对了消费者、挑错了调用路径**;这一次调用路径没错,只是消费者还不存在。

**二、32 位种子对隐藏信息的棋种偏窄。** 一个改过的客户端知道算法、看得见自己 17 张牌,可以枚举
2³² 个种子找出相符的那一个,从而算出另两家的牌 —— 一台笔记本上可行。不在本变更修,因为修它是换
洗牌算法,而那会改变所有历史对局的发牌;而这件事今天可以推迟,正是因为 `add-match-setup` 存的是
`Encode()` 的结果、不是种子。触发条件:第一次真实部署之前。

## 8. CI 红的那一条,与它的成因

- [x] 8.1 `AiSmoke` 的 `Count(g => !g.IsRated) == 1` 改成钉不变量,而不是把 1 改成 2
- [x] 8.2 斗地主自己的四个契约事实 + 步骤 9(建人房 201 / 建机器人房 400)
- [x] 8.3 400 正文不再让整个 smoke 崩掉
- [x] 8.4 变异验证:两个开关反过来 → 8 条红(含那条不变量);还原 → 35/35,退出码 0
- [x] 8.5 一处 live spec 漂移:`web-xiangqi` 的「唯一不计分」

### 后端 1266 全绿,而 CI 是红的

`dotnet test Gewu.slnx` 全绿,红的只有 `AiSmoke` 一条:

```
✗ tictactoe is the only unrated versus game
=== SUMMARY: 22 passed, 1 failed ===
```

**同一个事实的第二份副本,而第一份我当天就改了**(`GetGameDescriptorsQueryHandlerTests`
里那条已经写成"一字棋与斗地主,两者理由不同")。

成因**不是**这个仓库修过三遍的"手写清单冒充注册表" —— 这一份副本住在
**`dotnet test` 到不了的地方**:它是一个对着活服务器跑的控制台程序。
`ci/run-ai-smoke` 把它接进 CI 的理由,又一次由它自己给出。

### 改法不是把 1 改成 2

那只是把同一颗地雷往前挪一格:第九个棋种落地时它再红一次,而那一次同样只是"数字过期",
不带任何新信息。

分工现在是:**名册留在 Application 层**(那里两个不计分的棋种各写了不同的理由 —— 一字棋没有
人人对战,斗地主按分结算),`AiSmoke` 钉**不变量**(`IsRated ⇒ SupportsHumanVsHuman`)与
**两侧都非空**。于是它不必随棋种数量改动,而它守住的东西一点没少。

顺带补上的是**另一半**:上面那些断言是"客户端被告知了什么",步骤 9 是"服务端会接受什么"。
`enforce-human-vs-human` 与 `enforce-ai-availability` 的成因都是**从一半推出了另一半** ——
结论对 web UI 成立、对 API 不成立 —— 所以这两半必须分别量。

### 本地第一次跑,量的是前天的构建

`--no-build` 跑起来,`/hubs/match` 回 404,而 `/hubs/gomoku` 回 401 —— 那是
`rename-gomoku-to-gewu` **之前**的二进制,`bin/Release` 里的东西比源码旧两天。

**`--no-build` 量的是磁盘上恰好躺着的那份东西。** CI 不会踩到,因为它的 `--no-build` 紧跟在
同一个 job 里的 build 之后。更绕的一层紧接着出现:服务器还在跑的时候重新 build,MSBuild
锁文件失败(`error MSB3021 … 被 Gewu.Api (76080) 占用`),而**紧接着的 `--no-build` 会心满意足
地用那份没被替换掉的旧二进制** —— 一次失败的构建加一次成功的运行。

### 变异逼出来的两件事

**一、400 让 smoke 崩掉,而不是报告失败。** 把两个开关反过来之后建房返回 400,而 400 的正文是
`ProblemDetails`(`status` 是数字),于是解析抛异常、进程崩在那一行:CI 仍然红(退出码 127),
但**后面的断言一条都没报出来**。与本文件上面记的"smoke 死在它自己的落子冲突上,什么都没抓到"
是同一种坏掉的报告方式。改成只在 2xx 时解析之后,同一个变异报出 8 条红、退出码 1。

**二、一条空转通过的断言。** `waiting?.White is null` 在 `waiting` 本身为 `null` 时为真 ——
建房失败之后,它是唯一还绿着的那条。`waiting is { White: null }` 之后它跟着红。
**"房间没建出来"与"房间里那个座位是空的"必须是两个不同的结果。**

### 一处 live spec 漂移,而它一直是绿的

`web-xiangqi` 有一条 Scenario 叫「只有一字棋仍然没有排行榜入口」,理由写着
「它是唯一不计分的对战棋种」。斗地主落地之后那句话不成立。

**它的可执行形式一个字都不用改**:那条前端断言用的是桩、读的是 `isRated`,从来不数棋种个数。
也就是说**代码一直是对的,错的只有描述它的那句话** —— 而 `openspec validate --strict` 验形状、
不验真假,所以它验不出来。这与 `enable-xiangqi-human-play` 迟归档 36 个提交那次是同一类,
只是这次在同一个 PR 里就抓住了。

顺手核了另外两处带「唯一」的:`xiangqi/spec.md:166`(象棋自己不计分的唯一依据)与 `:172`
(一字棋仍是注册表里 `SupportsHumanVsHuman == false` 的那一个)—— **两条都仍然成立**,而能对
每一条说出**不同的**理由,才是"应用了规则"而不是"匹配了模式"的检验。

### 量到一件属于下一个变更的事

`Room.IsPlayer` 与 `Leave` 里的 `isPlayer` 读的是 `BlackPlayerId || WhitePlayerId`,也就是
**只有 0 号和 1 号座位**。三座位房间里 2 号座位上的人因此:离开房间会被拒
(`NotInRoomException`),而且**能把自己登记成围观者** —— 于是他会拿到围观视角、能发围观频道
的评论,正是 `fix-spectator-chat-leak` 建立的那条不变量。

不在本变更修:它是"按座位可见"的一部分,而 `PlayMove` 走的是 `SeatOf`(全部座位),所以出牌
这条路没问题。留给 `add-doudizhu-visibility`,写进它的 proposal。
