/**
 * 成语接龙's game key.
 *
 * Written down on the client for the same reason every other key is: the server
 * default that used to supply gomoku's key is gone (`require-room-game-key`), so
 * a key the client needs is a key the client states.
 */
export const IDIOM_CHAIN_KEY = 'idiom-chain';
